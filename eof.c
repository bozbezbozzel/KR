#include <stdio.h>

/*int main(void)
shows that getchar() != EOF evaluates to 1 
{
  printf("Press a key. ENTER would be nice :-)\n\n");
  printf("The expression getchar() != EOF evaluates to %d\n", getchar() != EOF);
  return 0;
} */

/* print value of EOF */
int main(void) {
	printf("The value of EOF is: %d\n", EOF);
}